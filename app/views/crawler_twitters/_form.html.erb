<%= form_for(@crawler_twitter) do |f| %>
  <% if @crawler_twitter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crawler_twitter.errors.count, "error") %> prohibited this crawler_twitter from being saved:</h2>

      <ul>
      <% @crawler_twitter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content, t('twitter.form.field.content') %>
    <%= f.text_area :content, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :user, t('twitter.form.field.user') %>
    <%= f.text_field :user, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :tags, t('twitter.form.field.tags') %>
    <%= f.text_field :tags, class: "form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :publish, t('twitter.form.field.publish') %>
    <%= f.datetime_select :publish, class: "form-control"%>
  </div>
  <div class="actions">
    <%= f.submit t('twitter.form.field.submit'), class: "btn btn-primary" %>
  </div>
<% end %>
